"use client";

import { useContext } from "react";
import { LoginModalContext } from "./login-modal-provider";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog";

export default function Page() {
  // const { isOpen, toggleModal } = useContext(LoginModalContext);
  return <div>Page</div>;
}
